# 📱 Android App Setup Guide\n\n## Step-by-Step Integration\n\n### 1️⃣ **Add Dependencies to build.gradle**\n\nOpen your `build.gradle` (Module: app) and add:\n\n```gradle\ndependencies {\n    // Retrofit & Networking\n    implementation 'com.squareup.retrofit2:retrofit:2.9.0'\n    implementation 'com.squareup.retrofit2:converter-gson:2.9.0'\n    implementation 'com.squareup.okhttp3:okhttp:4.11.0'\n    implementation 'com.squareup.okhttp3:logging-interceptor:4.11.0'\n    \n    // JSON Parsing\n    implementation 'com.google.code.gson:gson:2.10.1'\n    \n    // AndroidX\n    implementation 'androidx.appcompat:appcompat:1.6.1'\n    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'\n}\n```\n\nThen click **Sync Now**.\n\n---\n\n### 2️⃣ **Copy Java Files**\n\nCopy all Java files from this folder to your Android project:\n\n```\nandroid-integration/\n├── models/          → app/src/main/java/com/yourpackage/models/\n├── api/             → app/src/main/java/com/yourpackage/api/\n├── activities/      → app/src/main/java/com/yourpackage/activities/\n└── utils/           → app/src/main/java/com/yourpackage/utils/\n```\n\n**Replace `com.yourpackage` with your actual package name.**\n\n---\n\n### 3️⃣ **Copy Layout Files**\n\nCopy XML layout files to your project:\n\n```\nandroid-integration/layouts/\n├── activity_login.xml   → app/src/main/res/layout/\n└── activity_main.xml    → app/src/main/res/layout/\n```\n\n---\n\n### 4️⃣ **Update AndroidManifest.xml**\n\nAdd internet permission and register activities:\n\n```xml\n<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<manifest xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    package=\"com.yourpackage\">\n\n    <!-- Add Internet Permission -->\n    <uses-permission android:name=\"android.permission.INTERNET\" />\n\n    <application\n        android:allowBackup=\"true\"\n        android:icon=\"@mipmap/ic_launcher\"\n        android:label=\"@string/app_name\"\n        android:theme=\"@style/Theme.YourApp\">\n\n        <!-- Login Activity (Launcher) -->\n        <activity\n            android:name=\".activities.LoginActivity\"\n            android:exported=\"true\">\n            <intent-filter>\n                <action android:name=\"android.intent.action.MAIN\" />\n                <category android:name=\"android.intent.category.LAUNCHER\" />\n            </intent-filter>\n        </activity>\n\n        <!-- Main Activity -->\n        <activity\n            android:name=\".activities.MainActivity\"\n            android:exported=\"true\" />\n\n    </application>\n\n</manifest>\n```\n\n---\n\n### 5️⃣ **Update Constants.java**\n\nMake sure the base URL is correct:\n\n```java\npublic static final String BASE_URL = \"https://web-production-9be7c.up.railway.app\";\n```\n\n---\n\n### 6️⃣ **Create Edit Text Background (Optional)**\n\nCreate `app/src/main/res/drawable/edit_text_background.xml`:\n\n```xml\n<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<shape xmlns:android=\"http://schemas.android.com/apk/res/android\">\n    <solid android:color=\"#ffffff\" />\n    <stroke android:color=\"#cccccc\" android:width=\"1dp\" />\n    <corners android:radius=\"8dp\" />\n</shape>\n```\n\n---\n\n### 7️⃣ **Build and Run**\n\n1. Click **Build** → **Make Project**\n2. Click **Run** → **Run 'app'**\n3. Select your device/emulator\n4. App will launch with login screen\n\n---\n\n## 🧪 Testing\n\n### Test Login\n1. Open the app\n2. Generate a license key from the dashboard\n3. Enter the key in the login screen\n4. Click LOGIN\n5. You should see the main activity with your user info\n\n### Test Key Activation\n1. Generate another license key\n2. Enter it in the \"Activate License Key\" field\n3. Click ACTIVATE KEY\n4. Balance should update\n\n---\n\n## 🔧 Troubleshooting\n\n### \"Network Error\"\n- Check internet connection\n- Verify BASE_URL is correct\n- Check if server is running\n\n### \"Invalid Key\"\n- Ensure key is generated from dashboard\n- Check key hasn't expired\n- Verify key format\n\n### \"Build Error\"\n- Clean project: Build → Clean Project\n- Rebuild: Build → Rebuild Project\n- Check gradle sync\n\n### \"App Crashes on Launch\"\n- Check AndroidManifest.xml is updated\n- Verify all Java files are in correct packages\n- Check layout files are in res/layout/\n\n---\n\n## 📚 API Reference\n\n### Login with Key\n```java\nApiClient apiClient = new ApiClient();\napiClient.loginWithKey(\"YOUR_KEY\", new ApiClient.ApiCallback<LoginResponse>() {\n    @Override\n    public void onSuccess(LoginResponse response) {\n        if (response.isSuccess()) {\n            // User logged in\n            User user = response.getUser();\n        }\n    }\n    \n    @Override\n    public void onError(String error) {\n        // Handle error\n    }\n});\n```\n\n### Check Server Status\n```java\napiClient.checkServerStatus(new ApiClient.ApiCallback<ApiResponse>() {\n    @Override\n    public void onSuccess(ApiResponse response) {\n        // Server is active\n    }\n    \n    @Override\n    public void onError(String error) {\n        // Server is down\n    }\n});\n```\n\n### Use/Activate Key\n```java\napiClient.useKey(\"KEY_TO_ACTIVATE\", new ApiClient.ApiCallback<ApiResponse>() {\n    @Override\n    public void onSuccess(ApiResponse response) {\n        if (response.isSuccess()) {\n            double newBalance = response.getBalance();\n        }\n    }\n    \n    @Override\n    public void onError(String error) {\n        // Handle error\n    }\n});\n```\n\n---\n\n## 🎯 Next Steps\n\n1. ✅ Copy all files to your project\n2. ✅ Update package names\n3. ✅ Add dependencies\n4. ✅ Update AndroidManifest.xml\n5. ✅ Build and test\n6. ✅ Customize UI as needed\n\n---\n\n**Server:** https://web-production-9be7c.up.railway.app\n\n**Last Updated:** 2026-06-24\n
