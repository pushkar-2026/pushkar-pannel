package com.ashwin.vippanel.activities;\n\nimport android.content.Intent;\nimport android.os.Bundle;\nimport android.widget.Button;\nimport android.widget.EditText;\nimport android.widget.ProgressBar;\nimport android.widget.Toast;\nimport androidx.appcompat.app.AppCompatActivity;\nimport com.ashwin.vippanel.R;\nimport com.ashwin.vippanel.api.ApiClient;\nimport com.ashwin.vippanel.models.LoginResponse;\nimport com.ashwin.vippanel.utils.Constants;\nimport com.ashwin.vippanel.utils.SharedPrefManager;\n\n/**\n * Login Activity\n * Handles user login with license key\n */\npublic class LoginActivity extends AppCompatActivity {\n    private EditText etLicenseKey;\n    private Button btnLogin;\n    private ProgressBar progressBar;\n    private ApiClient apiClient;\n\n    @Override\n    protected void onCreate(Bundle savedInstanceState) {\n        super.onCreate(savedInstanceState);\n        setContentView(R.layout.activity_login);\n\n        // Check if already logged in\n        if (SharedPrefManager.getInstance(this).isLoggedIn()) {\n            startActivity(new Intent(this, MainActivity.class));\n            finish();\n            return;\n        }\n\n        // Initialize views\n        etLicenseKey = findViewById(R.id.et_license_key);\n        btnLogin = findViewById(R.id.btn_login);\n        progressBar = findViewById(R.id.progress_bar);\n\n        // Initialize API client\n        apiClient = new ApiClient();\n\n        // Set login button click listener\n        btnLogin.setOnClickListener(v -> performLogin());\n    }\n\n    /**\n     * Perform login with license key\n     */\n    private void performLogin() {\n        String key = etLicenseKey.getText().toString().trim();\n\n        // Validate input\n        if (key.isEmpty()) {\n            Toast.makeText(this, \"⚠️ Please enter license key\", Toast.LENGTH_SHORT).show();\n            return;\n        }\n\n        // Show progress\n        showProgress(true);\n\n        // Make API call\n        apiClient.loginWithKey(key, new ApiClient.ApiCallback<LoginResponse>() {\n            @Override\n            public void onSuccess(LoginResponse response) {\n                showProgress(false);\n\n                if (response.isSuccess()) {\n                    // Save user data\n                    SharedPrefManager.getInstance(LoginActivity.this)\n                            .saveUser(response.getUser());\n\n                    // Show success message\n                    Toast.makeText(\n                            LoginActivity.this,\n                            \"✅ Welcome \" + response.getUser().getUsername() + \"!\",\n                            Toast.LENGTH_SHORT\n                    ).show();\n\n                    // Navigate to main activity\n                    startActivity(new Intent(LoginActivity.this, MainActivity.class));\n                    finish();\n                } else {\n                    // Show error\n                    String error = response.getError() != null ?\n                            response.getError() : \"Login failed\";\n                    Toast.makeText(\n                            LoginActivity.this,\n                            \"❌ \" + error,\n                            Toast.LENGTH_SHORT\n                    ).show();\n                }\n            }\n\n            @Override\n            public void onError(String error) {\n                showProgress(false);\n                Toast.makeText(\n                        LoginActivity.this,\n                        \"❌ \" + error,\n                        Toast.LENGTH_SHORT\n                ).show();\n            }\n        });\n    }\n\n    /**\n     * Show or hide progress bar\n     * @param show true to show, false to hide\n     */\n    private void showProgress(boolean show) {\n        progressBar.setVisibility(show ? android.view.View.VISIBLE : android.view.View.GONE);\n        btnLogin.setEnabled(!show);\n        etLicenseKey.setEnabled(!show);\n    }\n}\n
